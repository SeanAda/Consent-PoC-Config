server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: https://consent-poc-eureka-server.cfapps.sap.hana.ondemand.com/eureka
  instance:
    preferIpAddress: false
    hostname: consent-poc-consent-gateway.cfapps.sap.hana.ondemand.com
    nonSecurePortEnabled: false
    securePort: 443
    securePortEnabled: true
    statusPageUrl: https://consent-poc-consent-gateway.cfapps.sap.hana.ondemand.com/info
    metadataMap:
      instanceId: ${spring.application.name}:${spring.application.instance_id:${eureka.instance.securePort}}
     
